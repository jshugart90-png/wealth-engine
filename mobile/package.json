{
  "name": "wealth-engine-mobile",
  "version": "1.0.1",
  "private": true,
  "description": "Capacitor wrappers for Wealth Engine games hub and freelancer tools",
  "scripts": {
    "sync:www": "node sync-www.mjs",
    "sync:games": "node sync-www.mjs games && cd games && npx cap sync",
    "sync:tools": "node sync-www.mjs tools && cd tools && npx cap sync",
    "android:games": "cd games && npx cap sync android && cd android && ./gradlew assembleDebug",
    "android:tools": "cd tools && npx cap sync android && cd android && ./gradlew assembleDebug",
    "ios:games": "cd games && npx cap sync ios",
    "ios:tools": "cd tools && npx cap sync ios",
    "open:android:games": "cd games && npx cap open android",
    "open:ios:games": "cd games && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.7.2"
  }
}
